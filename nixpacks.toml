# Pin Node 22 for Railway / any Nixpacks-driven CI.
# @xmtp/* packages require Node ≥ 22, and tsx 4.19 + Next 16 both work fine
# on Node 22 LTS. Pinning npm to 10.x to match the lockfile generator.
[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]
